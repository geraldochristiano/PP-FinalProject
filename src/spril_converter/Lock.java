package spril_converter;

/**
 * Representation of lock used for synchronization in (shared) memory.
 */
public class Lock extends MemoryContent{

    public Lock(String name){
        super(name);
    }
}
